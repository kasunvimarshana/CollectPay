export const config = {
  // For Android emulator, your host machine is usually reachable at 10.0.2.2
  // For a real device, point this to your LAN IP or a public HTTPS endpoint.
  apiBaseUrl: "http://10.0.2.2:8000/api/v1",
};
